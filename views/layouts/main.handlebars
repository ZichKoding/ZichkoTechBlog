<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta http-equiv="X-UA-Compatible" content="IE=edge" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <title>Zichko Tech Blog</title>
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous" />
        <link rel="stylesheet" href="/stylesheet/style.css" />
    </head>

    <body>
        <header>
            <div class="contiainer">
                <div class="row">
                    <div class="col-1"></div>
                    <h1 class="col-7 title">
                        <a href="/" class="h-links">Zichko Tech Blog</a>
                    </h1>
                    <nav class="col-4">
                        <div>
                            {{#if loggedIn}}
                            <a href="/dashboard" class="h-links nav-links">Dashboard</a>
                            <button class="h-links nav-links" id="logout">Logout</button>
                            {{else}}
                            <a href="/login" class="h-links nav-links">Login/Signup</a>
                            {{/if}}
                        </div>
                    </nav>
                </div>
            </div>    
        </header>
        <main>
            {{{body}}}
        </main>
        <footer>
            Thanks for visiting!
        </footer>
    </body>
    {{#if loggedIn}}
    <script src="/javascript/logout.js"></script>
    {{/if}}

</html>